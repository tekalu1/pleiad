# 検討・実験用の使い捨て置き場（追跡しない）
temporary/

node_modules/
dist/
out/
*.vsix
.env
.env.*
*.log
.DS_Store
Thumbs.db

# skills add が作る symlink（Windows では扱いにくい）。実体は .agents/ 側
.claude/skills/

# Claude Code のサブエージェント用 worktree（ハーネスが作る）
.claude/worktrees/

dist-desktop/

# 作業の出力とブラウザー自動化のログ（使い捨て）
output/
.playwright-cli/

# compact 仕様の下書き（リポジトリに入れない）
docs/compact-spec.md
