{
  "name": "pleiad-mobile",
  "private": true,
  "license": "Apache-2.0",
  "version": "0.1.0",
  "description": "Pleiad mobile shell (Capacitor). Host list, QR pairing and the in-app loopback proxy; the UI itself is served by the host.",
  "scripts": {
    "sync": "cap sync android",
    "build:debug": "cap sync android && cd android && gradlew assembleDebug",
    "test:jvm": "cd android && gradlew :remote-core:test"
  },
  "dependencies": {
    "@capacitor-mlkit/barcode-scanning": "8.2.1",
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
